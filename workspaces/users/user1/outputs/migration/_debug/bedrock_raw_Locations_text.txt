```json
{
  "entities": [
    {
      "entity": "Location",
      "migration_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Location\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"ingestion_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        source_files = [\"s3://amzn-s3-obq-bucket/workspaces/users/user1/uploads/locations.csv\"]\n        if any(str(f).startswith(\"s3://\") for f in source_files):\n            con.execute(\"INSTALL httpfs;\")\n            con.execute(\"LOAD httpfs;\")\n            s3_region = os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\", \"us-east-1\")\n            con.execute(f\"SET s3_region='{s3_region}';\")\n            s3_access = os.environ.get(\"S3_AWS_ACCESS_KEY_ID\") or os.environ.get(\"AWS_ACCESS_KEY_ID\")\n            s3_secret = os.environ.get(\"S3_AWS_SECRET_ACCESS_KEY\") or os.environ.get(\"AWS_SECRET_ACCESS_KEY\")\n            if s3_access and s3_secret:\n                con.execute(f\"SET s3_access_key_id='{s3_access}';\")\n                con.execute(f\"SET s3_secret_access_key='{s3_secret}';\")\n\n        con.execute(\"BEGIN TRANSACTION\")\n\n        con.execute(\"\"\"CREATE TABLE IF NOT EXISTS Location (\n                location_id VARCHAR(50) NOT NULL,\n                location_name VARCHAR(255) NOT NULL,\n                parent_id VARCHAR(50),\n                site VARCHAR(100),\n                building VARCHAR(100),\n                floor VARCHAR(50)\n            )\"\")\n\n        con.execute(\n            \"CREATE OR REPLACE TEMP TABLE staging AS \\\n            SELECT * FROM READ_CSV(?, header=true, union_by_name=true)\",\n            [source_files],\n        )\n        log(\"ingestion_end\", rows_read=con.execute(\"SELECT COUNT(*) FROM staging\").fetchone()[0])\n\n        log(\"transform_start\")\n        con.execute(\"DELETE FROM Location\")\n\n        con.execute(\"\"\"INSERT INTO Location (location_id, location_name, parent_id, site, building, floor)\n            SELECT\n                location_key,\n                loc_name,\n                parent_ref,\n                site,\n                building,\n                floor\n            FROM staging\"\"\")\n        log(\"transform_end\")\n\n        log(\"load_start\")\n        rows_written = con.execute(\"SELECT COUNT(*) FROM Location\").fetchone()[0]\n        con.execute(\"COMMIT\")\n        log(\"load_end\", rows_written=rows_written)\n\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"load_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "rollback_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Location\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"rollback_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        con.execute(\"BEGIN TRANSACTION\")\n        con.execute(\"DROP TABLE IF EXISTS Location\")\n        con.execute(\"COMMIT\")\n        log(\"rollback_end\")\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"rollback_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "validation_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Location\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"validation_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        log(\"checking_unique_location_id\")\n        duplicates = con.execute(\"SELECT COUNT(*) FROM Location GROUP BY location_id HAVING COUNT(*) > 1\").fetchone()[0]\n        assert duplicates == 0, f\"Duplicate location_id found: {duplicates}\"\n\n        log(\"checking_not_null_location_name\")\n        null_location_name = con.execute(\"SELECT COUNT(*) FROM Location WHERE location_name IS NULL\").fetchone()[0]\n        assert null_location_name == 0, f\"Null location_name found: {null_location_name}\"\n\n        log(\"validation_end\")\n    except Exception as e:\n        log(\"validation_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "depends_on": []
    }
  ]
}
```