```json
{
  "entities": [
    {
      "entity": "Asset",
      "migration_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Asset\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"ingestion_start\")\n    con = duckdb.connect(config.DUCKDB_PATH)\n    try:\n        source_files = [\"s3://amzn-s3-obq-bucket/workspaces/users/user1/uploads/assets.csv\"]\n        if any(str(f).startswith(\"s3://\") for f in source_files):\n            con.execute(\"INSTALL httpfs;\")\n            con.execute(\"LOAD httpfs;\")\n            s3_region = os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\", \"us-east-1\")\n            con.execute(f\"SET s3_region='{s3_region}';\")\n            s3_access = os.environ.get(\"S3_AWS_ACCESS_KEY_ID\") or os.environ.get(\"AWS_ACCESS_KEY_ID\")\n            s3_secret = os.environ.get(\"S3_AWS_SECRET_ACCESS_KEY\") or os.environ.get(\"AWS_SECRET_ACCESS_KEY\")\n            if s3_access and s3_secret:\n                con.execute(f\"SET s3_access_key_id='{s3_access}';\")\n                con.execute(f\"SET s3_secret_access_key='{s3_secret}';\")\n\n        con.execute(\"BEGIN TRANSACTION\")\n\n        con.execute(\"\"\"CREATE TABLE IF NOT EXISTS Asset (\n                c_asset_id VARCHAR(50) NOT NULL,\n                asset_name VARCHAR(255) NOT NULL,\n                b_location_id VARCHAR(50),\n                asset_type VARCHAR(100) NOT NULL,\n                status VARCHAR(50) NOT NULL,\n                purchase_date DATE,\n                cost DECIMAL(12,2)\n            )\"\"")\n\n        # SOURCE_FILES is a Python list — if it has more than one path,\n        # build a single READ_CSV call across all of them, e.g.:\n        #   con.execute(\"CREATE OR REPLACE TEMP TABLE staging AS \\n\"                \"SELECT * FROM READ_CSV(?, header=true)\", [SOURCE_FILES])\n        # (DuckDB's READ_CSV accepts a list of file paths directly.)\n        con.execute(\n            \"CREATE OR REPLACE TEMP TABLE staging AS \\n\"            \"SELECT * FROM READ_CSV(?, header=true, union_by_name=true)\",\n            [source_files],\n        )\n        log(\"ingestion_end\", rows_read=con.execute(\"SELECT COUNT(*) FROM staging\").fetchone()[0])\n\n        log(\"transform_start\")\n        # Idempotent: clear existing rows for this load before inserting\n        con.execute(\"DELETE FROM Asset\")\n\n        # Apply EVERY field mapping's transformation_logic here using SQL\n        # expressions inside the SELECT (e.g. UPPER(TRIM(asset_no)) AS c_asset_id).\n        con.execute(\"\"\"INSERT INTO Asset (c_asset_id, asset_name, b_location_id, asset_type, status, purchase_date, cost)\\n            SELECT\\n                UPPER(TRIM(asset_no)) AS c_asset_id,\\n                asset_name,\\n                loc_code AS b_location_id,\\n                asset_type,\\n                status,\\n                cast(purchase_date, 'date') AS purchase_date,\\n                cast(cost, 'decimal') AS cost\\n            FROM staging\"\"")\n        log(\"transform_end\")\n\n        log(\"load_start\")\n        rows_written = con.execute(\"SELECT COUNT(*) FROM Asset\").fetchone()[0]\n        con.execute(\"COMMIT\")\n        log(\"load_end\", rows_written=rows_written)\n\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"load_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()\n",
      "rollback_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Asset\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"rollback_start\")\n    con = duckdb.connect(config.DUCKDB_PATH)\n    try:\n        con.execute(\"BEGIN TRANSACTION\")\n        con.execute(\"DROP TABLE IF EXISTS Asset\")\n        con.execute(\"COMMIT\")\n        log(\"rollback_end\")\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"rollback_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()\n",
      "validation_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"Asset\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"validation_start\")\n    con = duckdb.connect(config.DUCKDB_PATH)\n    try:\n        # Validation Rule: c_asset_id IS_UNIQUE\n        result = con.execute(\"SELECT COUNT(*) FROM Asset GROUP BY c_asset_id HAVING COUNT(*) > 1\").fetchone()\n        assert result[0] == 0, \"c_asset_id is not unique\"\n\n        # Validation Rule: asset_name NOT NULL\n        result = con.execute(\"SELECT COUNT(*) FROM Asset WHERE asset_name IS NULL\").fetchone()\n        assert result[0] == 0, \"asset_name contains null values\"\n\n        # Validation Rule: asset_type NOT NULL\n        result = con.execute(\"SELECT COUNT(*) FROM Asset WHERE asset_type IS NULL\").fetchone()\n        assert result[0] == 0, \"asset_type contains null values\"\n\n        # Validation Rule: status NOT NULL\n        result = con.execute(\"SELECT COUNT(*) FROM Asset WHERE status IS NULL\").fetchone()\n        assert result[0] == 0, \"status contains null values\"\n\n        # Validation Rule: purchase_date IS_DATE\n        result = con.execute(\"SELECT COUNT(*) FROM Asset WHERE purchase_date IS NOT NULL AND NOT is_date(purchase_date)\").fetchone()\n        assert result[0] == 0, \"purchase_date contains invalid date values\"\n\n        # Validation Rule: cost >= 0\n        result = con.execute(\"SELECT COUNT(*) FROM Asset WHERE cost IS NOT NULL AND cost < 0\").fetchone()\n        assert result[0] == 0, \"cost contains negative values\"\n\n        log(\"validation_end\", status=\"PASSED\")\n    except AssertionError as e:\n        log(\"validation_end\", status=\"FAILED\", error_code=\"AssertionError\", error_message=str(e))\n        raise\n    except Exception as e:\n        log(\"validation_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()\n",
      "depends_on": ["Users", "Locations", "WorkOrders"]
    }
  ]
}
```