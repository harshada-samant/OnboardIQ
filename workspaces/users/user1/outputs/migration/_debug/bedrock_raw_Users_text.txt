```json
{
  "entities": [
    {
      "entity": "User",
      "migration_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"User\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"ingestion_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        source_files = [\"s3://amzn-s3-obq-bucket/workspaces/users/user1/uploads/users.csv\"]\n        if any(str(f).startswith(\"s3://\") for f in source_files):\n            con.execute(\"INSTALL httpfs;\")\n            con.execute(\"LOAD httpfs;\")\n            s3_region = os.environ.get(\"AWS_REGION\") or os.environ.get(\"AWS_DEFAULT_REGION\", \"us-east-1\")\n            con.execute(f\"SET s3_region='{s3_region}';\")\n            s3_access = os.environ.get(\"S3_AWS_ACCESS_KEY_ID\") or os.environ.get(\"AWS_ACCESS_KEY_ID\")\n            s3_secret = os.environ.get(\"S3_AWS_SECRET_ACCESS_KEY\") or os.environ.get(\"AWS_SECRET_ACCESS_KEY\")\n            if s3_access and s3_secret:\n                con.execute(f\"SET s3_access_key_id='{s3_access}';\")\n                con.execute(f\"SET s3_secret_access_key='{s3_secret}';\")\n\n        con.execute(\"BEGIN TRANSACTION\")\n\n        con.execute(\"\"\"CREATE TABLE IF NOT EXISTS Users (\n                user_id VARCHAR(50) NOT NULL,\n                full_name VARCHAR(255) NOT NULL,\n                role VARCHAR(100),\n                email VARCHAR(255),\n                department VARCHAR(100),\n                is_active BOOLEAN\n            )\"\")\n\n        con.execute(\n            \"CREATE OR REPLACE TEMP TABLE staging AS \"\n            \"SELECT * FROM READ_CSV(?, header=true, union_by_name=true)\",\n            [source_files],\n        )\n        log(\"ingestion_end\", rows_read=con.execute(\"SELECT COUNT(*) FROM staging\").fetchone()[0])\n\n        log(\"transform_start\")\n        con.execute(\"DELETE FROM Users\")\n\n        con.execute(\"\"\"\n            INSERT INTO Users (user_id, full_name, role, email, department, is_active)\n            SELECT\n                user_id,\n                name,\n                role,\n                email,\n                department,\n                active\n            FROM staging\n        \"\"\")\n        log(\"transform_end\")\n\n        log(\"load_start\")\n        rows_written = con.execute(\"SELECT COUNT(*) FROM Users\").fetchone()[0]\n        con.execute(\"COMMIT\")\n        log(\"load_end\", rows_written=rows_written)\n\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"load_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "rollback_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"User\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"rollback_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        con.execute(\"BEGIN TRANSACTION\")\n        con.execute(\"DROP TABLE IF EXISTS Users\")\n        con.execute(\"COMMIT\")\n        log(\"rollback_end\")\n    except Exception as e:\n        con.execute(\"ROLLBACK\")\n        log(\"rollback_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "validation_script": "import duckdb\nimport os\nimport sys\nimport json\nfrom datetime import datetime, timezone\n\nRUN_ID = os.environ.get(\"RUN_ID\", \"manual\")\nENTITY = \"User\"\n\ndef log(stage, **fields):\n    print(json.dumps({\"run_id\": RUN_ID, \"entity\": ENTITY, \"stage\": stage,\n                       \"timestamp\": datetime.now(timezone.utc).isoformat(), **fields}), flush=True)\n\ndef main():\n    log(\"validation_start\")\n    con = duckdb.connect(\"main.db\")\n    try:\n        # Check row count\n        expected_row_count = con.execute(\"SELECT COUNT(*) FROM staging\").fetchone()[0]\n        actual_row_count = con.execute(\"SELECT COUNT(*) FROM Users\").fetchone()[0]\n        assert expected_row_count == actual_row_count, f\"Row count mismatch: Expected {expected_row_count}, Actual {actual_row_count}\"\n\n        # Check for duplicate user_ids\n        duplicates = con.execute(\"SELECT user_id FROM Users GROUP BY user_id HAVING COUNT(*) > 1\").fetchall()\n        assert len(duplicates) == 0, f\"Duplicate user_ids found: {duplicates}\"\n\n        # Add more validation rules here based on SPECIFICATION\n        # Example: Check for null values in required fields\n        null_check = con.execute(\"SELECT * FROM Users WHERE full_name IS NULL\").fetchall()\n        assert len(null_check) == 0, f\"Null values found in full_name: {null_check}\"\n\n        log(\"validation_end\", status=\"PASSED\")\n    except AssertionError as e:\n        log(\"validation_end\", status=\"FAILED\", error_code=\"AssertionError\", error_message=str(e))\n        raise\n    except Exception as e:\n        log(\"validation_end\", status=\"FAILED\", error_code=type(e).__name__, error_message=str(e))\n        raise\n    finally:\n        con.close()\n\nif __name__ == \"__main__\":\n    main()",
      "depends_on": []
    }
  ]
}
```